# Vercel Python Serverless uses built-in standard library (json, math, http.server)
# Heavy ML training packages (scikit-learn, pandas, numpy) are in ml_model/requirements.txt for local model training
